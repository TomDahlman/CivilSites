<%= form_for(@wonder) do |f| %>
  <% if @wonder.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wonder.errors.count, "error") %> prohibited this wonder from being saved:</h2>

      <ul>
      <% @wonder.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :notes %><br />
    <%= f.text_field :notes %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
